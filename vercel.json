{
  "buildCommand": "cd quijano-client && npm install && npm run build",
  "outputDirectory": "quijano-client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
